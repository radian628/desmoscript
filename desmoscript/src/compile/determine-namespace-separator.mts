import { DesmoscriptCompileContext } from "../semantic-analysis/analysis-types.mjs";

// determines what string to use for separating namespaces while completely
// avoiding namespace collisions
function determineNamespaceSeparator(ctx: DesmoscriptCompileContext) {
  for (const unit of ctx.compilationUnits) {
    
  }
}