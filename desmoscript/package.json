{
    "name": "desmoscript",
    "version": "0.1.12",
    "scripts": {
        "nearley": "npx nearleyc src/grammar/grammar.ne -o src/grammar/grammar.mts",
        "dev": "npx tsc --watch",
        "build": "npx tsc",
        "prepare": "npm run nearley && npm run build"
    },
    "devDependencies": {
        "@types/nearley": "^2.11.2"
    },
    "dependencies": {
        "nearley": "^2.20.1"
    }
}
